[package]
name = "fast_ops"
version = "0.1.0"
edition = "2021"

[lib]
name = "fast_ops"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
rayon = "1.10"
num_cpus = "1.16"